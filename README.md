Run with hardhat :
<br>first run the node with npx hard hat node 
<br>deploy the contract with npx hardhat run scripts/deploy.js --network localhost
<br>then  open the front end and run the app with npm run dev
<br>*note check the address first after you deploy and change the address with deployed in frontend/src/lib/contract.ts
