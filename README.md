Run with hardhat : 
first run the node with npx hard hat node 
deploy the contract with npx hardhat run scripts/deploy.js --network localhost
then  open the front end and run the app with npm run dev
*note check the address first after you deploy and change the address with deployed in frontend/src/lib/contract.ts
